[pytest]
# Tell pytest-django which Django settings module to load.
# This is the equivalent of setting DJANGO_SETTINGS_MODULE in the shell.
DJANGO_SETTINGS_MODULE = config.settings

# Pattern for test file discovery — pytest will find files matching this name.
python_files = test_*.py

# Default command-line options applied to every pytest run:
#   --tb=short  — compact traceback format (easier to read than the default)
#
# Keep coverage gates in explicit CI/developer commands rather than global
# addopts so focused runs like `pytest apps/cv/tests/...` are not failed by
# unrelated accounts/parking coverage measurement.
addopts = --tb=short

markers =
    unit: fast, isolated unit tests with no I/O or database
    integration: tests that hit the database or external services
